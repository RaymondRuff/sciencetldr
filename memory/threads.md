# Running threads

This file is written by `scripts/memory.py` once the back-catalogue backfill has
run (Actions -> memory -> Run workflow). It holds the recurring debates the show
returns to across episodes, and the hosts read it when recording.

It is meant to be hand-edited: if a thread is wrong or no longer interesting,
change it here and the next update will build on your version.
